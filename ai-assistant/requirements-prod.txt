# 生产智合（智谱远程 API）。不含本机 Ollama / fastembed，省内存。
# 只选 CentOS 7（glibc 2.17）上有 manylinux2014 预编译包的版本，避免源码编译。
# 不要用 uvicorn[standard]：其中 httptools/uvloop 在 CentOS 7 会走编译。

langchain-core>=0.2.0,<0.4
langchain-openai>=0.1.0,<0.3
langgraph>=0.1.0,<0.3
openai>=1.40.0,<2
tiktoken==0.7.0
python-dotenv>=1.0.0,<2
pydantic>=2.7.0,<2.11
fastapi>=0.111.0,<0.116
uvicorn>=0.30.0,<0.33
requests>=2.31.0,<3
pypdf>=4.2.0,<6
