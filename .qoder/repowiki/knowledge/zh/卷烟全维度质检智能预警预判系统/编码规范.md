- 服务层方法普遍采用静态工具类风格（如 `SPCAnalysisService.analyzeAll()`），不依赖Spring Bean注入，直接调用工具类获取数据。
- 控制器统一返回 `Map<String, Object>` 或实体列表，通过 `LinkedHashMap` 保证JSON响应字段的顺序性以适配前端图表渲染。
- 常量定义集中在 `DefectConstants` 类中，包括SPC控制限（UCL/LCL）、缺陷颜色代码及物理指标中心值。